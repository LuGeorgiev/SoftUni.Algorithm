using System;

namespace StronglyConnectedGraph
{
    //Each two nodes have to have connection in order a Graph to be strongly connected
    //Kosaraju-Sharir Algorithm
    //Oriented Graph is needed
    // Foreach node traverse with DFS (visiting each node ONCE)
    //Each time DFS finishes (before recursive return) we push nodes in a STACK
    class Program
    {
        static void Main(string[] args)
        {
            
        }
    }
}
